npx @bookshop/generate
cd _site/
echo "Contents of _site"
ls
cd _cloudcannon
echo "Contents of _site/_cloudcannon"
ls
cd ..
cd ..
echo "Back in root, contents are:"
ls

# Rosey
npx rosey generate --source _site
npx rosey-cloudcannon-connector write-locales --source rosey --dest _site --locales fr

mv ./_site ./_untranslated_site
npx rosey build --source _untranslated_site --dest _site --default-language-at-root
cp -r _untranslated_site/_rcc _site/_rcc
# mkdir _site/_cloudcannon
# echo "{}" > _site/_cloudcannon/info.json
